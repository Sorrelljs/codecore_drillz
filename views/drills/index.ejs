<%- include('../partials/header') %>

<h1 class="m-5">Drill </h1>

<div class='d-flex justify-content-end'>
    <a class="nav-link btn btn-secondary" href="/drills/new">Add Drill</a>
</div>

<% for (let drill of drillz) {%>
    <div class='card'>
        <div class='card-body'>
            <div class="d-flex justify-content-between">
                <h5 class="card-title"><%= drill.title %></h5>
            </div>
            <div class='card-text'>
                <%= drill.description %> 
            </div>
        </div>
        <div class="d-flex justify-content-end">
            <form action="/drill_groups/<%= drill.id %>" method="GET">
                <button class="btn btn-secondary" type="submit">View</button>
            </form>
            <form action="/drill_groups/<%=  drill.id %>/edit" method="GET">
                <button class="btn btn-secondary" type="submit">Edit</button>
            </form>
            <form action="/drill_groups/<%=  drill.id %>" method="POST">
                <input type="hidden" name="_method" value="DELETE">
                <button class="btn btn-secondary" type="submit">Delete</button>
            </form>
        </div>
    </div>
<% } %> 


<%- include('../partials/footer') %>