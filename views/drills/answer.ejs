<%- include('../partials/header') %>

<div class="container m-3">
    <div class="card-body">
        <% if (student_answer.toLowerCase() == drill_answer.toLowerCase()) { %>
            <h5 class="card-title">Correct!</h5>
        <% } else { %> 
            <h5 class="card-title">Sorry that's not correct :(</h5>
        <% } %> 
    </div>
</div>
<%- include('../partials/footer') %>